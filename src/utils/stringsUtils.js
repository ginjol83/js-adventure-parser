/**
 * Convierte todas las cadenas en un arreglo a minúsculas.
 * @param {string} arr - El arreglo de cadenas a convertir.
 * @returns {string} - Un nuevo arreglo con todas las cadenas en minúsculas.
 */
const toLowerCase = (arr) => arr.toLowerCase()

export { toLowerCase } 